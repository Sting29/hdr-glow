// A short version of "What it does", meant to sit before the tool itself, so
// a first-time visitor knows what they're looking at before they touch it.
// The longer explanation lives in Explainer, after both demos.

export function Features() {
  return (
    <ul className="features">
      <li>Runs entirely in this browser tab: no server, no account, no upload.</li>
      <li>Upload a logo, pick which colors should glow, download an HDR JPEG.</li>
      <li>Makes text on your own site glow too, with a few lines of CSS.</li>
    </ul>
  );
}
